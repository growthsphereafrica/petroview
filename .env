# Backend API base URL for the Master View cloud gateway.
# Leave empty to run in simulated-sync (demo) mode.
# Use your PC's LAN IP so phones/tablets on the same WiFi can call the API too.
VITE_API_URL=http://192.168.100.47:4000